"use client";
import AppPortalLayout from "@/app/student/layout";
export default function Layout({ children }: { children: React.ReactNode }) {
  return <AppPortalLayout>{children}</AppPortalLayout>;
}
